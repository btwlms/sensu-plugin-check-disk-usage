sensu-plugin-check-disk-usage
=============================

Sensu plugin for checking specified file/directory's disk usage.

Usage
-----------------------------

Put `check-disk-usage.rb` onto `/etc/sensu/plugins`!

Testing
-----------------------------

```
git clone
cd
bundle install --path vendor/bundle
bundle exec rspec
```
